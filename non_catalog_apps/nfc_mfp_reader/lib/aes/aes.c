/**
 * tiny-AES-c — AES-128 ECB and CBC
 * Public domain. Stripped to AES-128, no AES-192/256.
 */
#include "aes.h"
#include <string.h>

#define Nb 4
#define Nk 4
#define Nr 10

typedef uint8_t state_t[4][4];

static const uint8_t sbox[256] = {
    
};

static const uint8_t rsbox[256] = {
    
};

static const uint8_t Rcon[11] = {
    0x8d,0x01,0x02,0x04,0x08,0x10,0x20,0x40,0x80,0x1b,0x36
};

#define xtime(x) ((uint8_t)(((x) << 1) ^ ((((x) >> 7) & 1) * 0x1b)))
#define Multiply(x,y) \
    ((((y) & 1) * (x)) ^ \
     (((y)>>1 & 1) * xtime(x)) ^ \
     (((y)>>2 & 1) * xtime(xtime(x))) ^ \
     (((y)>>3 & 1) * xtime(xtime(xtime(x)))) ^ \
     (((y)>>4 & 1) * xtime(xtime(xtime(xtime(x))))))

static void KeyExpansion(uint8_t* RoundKey, const uint8_t* Key) {
    uint8_t tempa[4];
    for(unsigned i = 0; i < Nk; i++) {
        RoundKey[(i*4)+0] = Key[(i*4)+0];
        RoundKey[(i*4)+1] = Key[(i*4)+1];
        RoundKey[(i*4)+2] = Key[(i*4)+2];
        RoundKey[(i*4)+3] = Key[(i*4)+3];
    }
    for(unsigned i = Nk; i < Nb*(Nr+1); i++) {
        unsigned k = (i-1)*4;
        tempa[0]=RoundKey[k+0]; tempa[1]=RoundKey[k+1];
        tempa[2]=RoundKey[k+2]; tempa[3]=RoundKey[k+3];
        if(i % Nk == 0) {
            uint8_t u8tmp = tempa[0];
            tempa[0] = sbox[tempa[1]] ^ Rcon[i/Nk];
            tempa[1] = sbox[tempa[2]];
            tempa[2] = sbox[tempa[3]];
            tempa[3] = sbox[u8tmp];
        }
        k = (i-Nk)*4;
        RoundKey[i*4+0] = RoundKey[k+0] ^ tempa[0];
        RoundKey[i*4+1] = RoundKey[k+1] ^ tempa[1];
        RoundKey[i*4+2] = RoundKey[k+2] ^ tempa[2];
        RoundKey[i*4+3] = RoundKey[k+3] ^ tempa[3];
    }
}

void AES_init_ctx(struct AES_ctx* ctx, const uint8_t* key) {
    KeyExpansion(ctx->RoundKey, key);
}

void AES_init_ctx_iv(struct AES_ctx* ctx, const uint8_t* key, const uint8_t* iv) {
    KeyExpansion(ctx->RoundKey, key);
    memcpy(ctx->Iv, iv, AES_BLOCKLEN);
}

void AES_ctx_set_iv(struct AES_ctx* ctx, const uint8_t* iv) {
    memcpy(ctx->Iv, iv, AES_BLOCKLEN);
}

static void AddRoundKey(uint8_t round, state_t* state, const uint8_t* RoundKey) {
    for(uint8_t i = 0; i < 4; i++)
        for(uint8_t j = 0; j < 4; j++)
            (*state)[i][j] ^= RoundKey[(round*Nb*4) + (i*Nb) + j];
}

static void SubBytes(state_t* state) {
    for(uint8_t i = 0; i < 4; i++)
        for(uint8_t j = 0; j < 4; j++)
            (*state)[j][i] = sbox[(*state)[j][i]];
}

static void ShiftRows(state_t* state) {
    uint8_t temp;
    temp           = (*state)[0][1];
    (*state)[0][1] = (*state)[1][1];
    (*state)[1][1] = (*state)[2][1];
    (*state)[2][1] = (*state)[3][1];
    (*state)[3][1] = temp;

    temp           = (*state)[0][2];
    (*state)[0][2] = (*state)[2][2];
    (*state)[2][2] = temp;
    temp           = (*state)[1][2];
    (*state)[1][2] = (*state)[3][2];
    (*state)[3][2] = temp;

    temp           = (*state)[0][3];
    (*state)[0][3] = (*state)[3][3];
    (*state)[3][3] = (*state)[2][3];
    (*state)[2][3] = (*state)[1][3];
    (*state)[1][3] = temp;
}

static void MixColumns(state_t* state) {
    for(uint8_t i = 0; i < 4; i++) {
        uint8_t Tmp  = (*state)[i][0];
        uint8_t Tm   = (*state)[i][0] ^ (*state)[i][1] ^ (*state)[i][2] ^ (*state)[i][3];
        uint8_t t    = (*state)[i][0] ^ (*state)[i][1];
        t = xtime(t); (*state)[i][0] ^= t ^ Tm;
        t = (*state)[i][1] ^ (*state)[i][2]; t = xtime(t); (*state)[i][1] ^= t ^ Tm;
        t = (*state)[i][2] ^ (*state)[i][3]; t = xtime(t); (*state)[i][2] ^= t ^ Tm;
        t = (*state)[i][3] ^ Tmp;            t = xtime(t); (*state)[i][3] ^= t ^ Tm;
    }
}

static void InvMixColumns(state_t* state) {
    for(uint8_t i = 0; i < 4; i++) {
        uint8_t a = (*state)[i][0], b = (*state)[i][1],
                c = (*state)[i][2], d = (*state)[i][3];
        (*state)[i][0] = Multiply(a,0x0e) ^ Multiply(b,0x0b) ^ Multiply(c,0x0d) ^ Multiply(d,0x09);
        (*state)[i][1] = Multiply(a,0x09) ^ Multiply(b,0x0e) ^ Multiply(c,0x0b) ^ Multiply(d,0x0d);
        (*state)[i][2] = Multiply(a,0x0d) ^ Multiply(b,0x09) ^ Multiply(c,0x0e) ^ Multiply(d,0x0b);
        (*state)[i][3] = Multiply(a,0x0b) ^ Multiply(b,0x0d) ^ Multiply(c,0x09) ^ Multiply(d,0x0e);
    }
}

static void InvSubBytes(state_t* state) {
    for(uint8_t i = 0; i < 4; i++)
        for(uint8_t j = 0; j < 4; j++)
            (*state)[j][i] = rsbox[(*state)[j][i]];
}

static void InvShiftRows(state_t* state) {
    uint8_t temp;
    temp           = (*state)[3][1];
    (*state)[3][1] = (*state)[2][1];
    (*state)[2][1] = (*state)[1][1];
    (*state)[1][1] = (*state)[0][1];
    (*state)[0][1] = temp;

    temp           = (*state)[0][2];
    (*state)[0][2] = (*state)[2][2];
    (*state)[2][2] = temp;
    temp           = (*state)[1][2];
    (*state)[1][2] = (*state)[3][2];
    (*state)[3][2] = temp;

    temp           = (*state)[0][3];
    (*state)[0][3] = (*state)[1][3];
    (*state)[1][3] = (*state)[2][3];
    (*state)[2][3] = (*state)[3][3];
    (*state)[3][3] = temp;
}

static void Cipher(state_t* state, const uint8_t* RoundKey) {
    AddRoundKey(0, state, RoundKey);
    for(uint8_t round = 1; ; round++) {
        SubBytes(state);
        ShiftRows(state);
        if(round == Nr) break;
        MixColumns(state);
        AddRoundKey(round, state, RoundKey);
    }
    AddRoundKey(Nr, state, RoundKey);
}

static void InvCipher(state_t* state, const uint8_t* RoundKey) {
    AddRoundKey(Nr, state, RoundKey);
    for(uint8_t round = Nr-1; ; round--) {
        InvShiftRows(state);
        InvSubBytes(state);
        AddRoundKey(round, state, RoundKey);
        if(round == 0) break;
        InvMixColumns(state);
    }
}

void AES_ECB_encrypt(const struct AES_ctx* ctx, uint8_t* buf) {
    Cipher((state_t*)buf, ctx->RoundKey);
}

void AES_ECB_decrypt(const struct AES_ctx* ctx, uint8_t* buf) {
    InvCipher((state_t*)buf, ctx->RoundKey);
}

void AES_CBC_encrypt_buffer(struct AES_ctx* ctx, uint8_t* buf, size_t length) {
    uint8_t* iv = ctx->Iv;
    for(size_t i = 0; i < length; i += AES_BLOCKLEN) {
        for(uint8_t j = 0; j < AES_BLOCKLEN; j++)
            buf[i+j] ^= iv[j];
        Cipher((state_t*)(buf+i), ctx->RoundKey);
        iv = buf + i;
    }
    memcpy(ctx->Iv, iv, AES_BLOCKLEN);
}

void AES_CBC_decrypt_buffer(struct AES_ctx* ctx, uint8_t* buf, size_t length) {
    uint8_t storeNextIv[AES_BLOCKLEN];
    for(size_t i = 0; i < length; i += AES_BLOCKLEN) {
        memcpy(storeNextIv, buf+i, AES_BLOCKLEN);
        InvCipher((state_t*)(buf+i), ctx->RoundKey);
        for(uint8_t j = 0; j < AES_BLOCKLEN; j++)
            buf[i+j] ^= ctx->Iv[j];
        memcpy(ctx->Iv, storeNextIv, AES_BLOCKLEN);
    }
}
