# Build artefacts
.vscode/
.ufbt/
build/
dist/
*.fap
*.elf
*.o
*.d

# Editor / OS
*.swp
.DS_Store
.idea/

# Vendored upstream sources used as a reference only — not part of the
# distribution (heavy, GPL-licensed in places, and not shipped in our
# .fap). Re-clone locally if you want to cross-check decoders.
_refs/

# Compiled host-test binaries (regenerated by `make -C tests check`)
tests/test_izar
tests/test_ports
tests/test_*.o

